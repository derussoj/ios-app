//
//  Settings.swift
//  Cheers
//
//  Created by Air on 5/9/15.
//  Copyright (c) 2015 Cheers. All rights reserved.
//

import UIKit

// not sure I ever actually used this class
/*
class Settings: NSObject, NSCoding
{
    var bacEstimation: Bool!
    var weight: Double!
    var sex: Int!
    
    var location: Bool!
    
    init(bacEstimation: Bool, weight: Double, sex: Int, location: Bool)
    {
        super.init()
        self.bacEstimation = bacEstimation
        self.weight = weight
        self.sex = sex
        self.location = location
    }
    
    required init?(coder decoder: NSCoder)
    {
        super.init()
        self.bacEstimation = decoder.decodeObject(forKey: "bacEstimation") as! Bool
        self.weight = decoder.decodeObject(forKey: "weight") as! Double
        self.sex = decoder.decodeObject(forKey: "sex") as! Int
        self.location = decoder.decodeObject(forKey: "location") as! Bool
    }
    
    func encodeWithCoder(aCoder: NSCoder)
    {
        aCoder.encode(bacEstimation, forKey: "bacEstimation")
        aCoder.encode(weight, forKey: "weight")
        aCoder.encode(sex, forKey: "sex")
        aCoder.encode(location, forKey: "location")
    }
}
*/
